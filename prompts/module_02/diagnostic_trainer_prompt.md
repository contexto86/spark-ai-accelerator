# Module 02 Trainer Diagnostic Prompt

Begin with reasoning, not syntax. Ask the learner to predict how Spark SQL differs from PostgreSQL, what a temporary view is, what happens when a query references data spread across partitions, and why a join might become expensive. Ask for predictions before showing commands.

