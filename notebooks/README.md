# Notebooks

Run Module 02 notebooks from the repository root with the Docker setup:

```bash
docker compose -f docker/docker-compose.yml up --build
```

Then open JupyterLab and use `notebooks/module_02/`. The notebooks read the small CSV files in `datasets/module_02/`.

