# Exercise 03: Select and Filter

Create a DataFrame of residential transactions with only `transaction_id`, `municipality_id`, `sale_price`, `sale_date`, and `property_type`. Keep `apartment`, `detached_house`, and `row_house` where `sale_price >= 500000`.

Explain how projection and filtering differ physically.
