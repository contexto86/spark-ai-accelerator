# Exercise 05: Aggregations

Aggregate transactions by `municipality_id` and `property_type`. Compute transaction count, average sale price, max sale price, and total sale value.

Predict the partial aggregate, `Exchange`, and final aggregate pattern before running `explain("formatted")`.
