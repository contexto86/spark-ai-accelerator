# Exercise 04: Create Columns

Add `sale_year`, `sale_price_millions`, and `price_band` to transactions. Use built-in functions only. Price bands: `premium` for prices >= 1000000, `standard` for 500000-999999, and `entry` below 500000.

Explain why `when` is preferable to a Python UDF here.
