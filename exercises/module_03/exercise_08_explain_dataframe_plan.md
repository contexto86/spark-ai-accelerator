# Exercise 08: Explain a DataFrame Plan

Build a DataFrame pipeline that joins transactions to municipalities, groups by canton and property type, computes average sale price, and orders by average sale price descending.

Run `explain("formatted")`. Identify scans, projects, joins, exchanges, aggregates, sorts, and adaptive execution.
