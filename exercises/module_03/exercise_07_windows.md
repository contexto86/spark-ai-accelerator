# Exercise 07: Window Functions

Rank transactions within each canton by descending sale price, using `transaction_id` as a deterministic tie-breaker. Return the top 3 transactions per canton.

Explain why this is a window problem rather than a `GROUP BY` problem.
