# Exercise 02: Load Module 03 Data

Load `datasets/module_03/transactions.csv` and `datasets/module_03/population_history.csv` using explicit schemas. Parse `sale_date` into a date column. Create temp views for both DataFrames.

Verify with `printSchema()`, `show(5)`, and `count()`. State which calls trigger execution.
