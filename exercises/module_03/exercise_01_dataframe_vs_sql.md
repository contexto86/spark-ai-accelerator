# Exercise 01: DataFrame vs SQL Mental Model

Use `municipalities` from Module 02. Write the SQL and DataFrame versions of selecting `municipality_id`, `municipality_name`, and `canton`.

Before coding, explain whether either version should execute immediately. Then run `explain("formatted")` for the DataFrame version and identify the `Scan` and `Project` nodes.
